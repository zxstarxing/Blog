extends layout

block content
    .container
        ul.list-group
            each article, i in articles
                li.list-group-item
                    a(href="/articles/content/"+article._id)= article.title